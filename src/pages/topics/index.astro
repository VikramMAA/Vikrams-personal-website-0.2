---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import CTA from '../../components/CTA.astro';
import { site, topics } from '../../data/site';
import { breadcrumbSchema } from '../../data/schema';

const title = 'Topics — SEO, Ads, Social, Content & Lead Generation Notes';
const description = `The disciplines ${site.name} writes about: SEO, performance marketing, social media, content marketing, lead generation, and marketing and sales alignment.`;

const crumbs = [
  { label: 'Home', href: '/' },
  { label: 'Topics', href: '/topics/' },
];

const itemList = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: 'Digital marketing topics',
  itemListElement: topics.map((t, i) => ({
    '@type': 'ListItem',
    position: i + 1,
    name: t.title,
    description: t.summary,
    url: `${site.url}/topics/${t.slug}/`,
  })),
};
---

<BaseLayout
  title={title}
  description={description}
  schema={[breadcrumbSchema(crumbs), itemList]}
>
  <Breadcrumbs items={crumbs} />

  <section class="section">
    <div class="container">
      <div class="section-head">
        <p class="eyebrow">Topics</p>
        <h1>What I write about</h1>
        <p>
          Six disciplines I have spent ten years inside. Each page is a set of notes:
          the pattern I keep running into, what actually moves the number, and how you
          know it is working. Opinions, not offers.
        </p>
      </div>

      <div class="topic-list">
        {
          topics.map((topic, i) => (
            <article class="topic-row">
              <div class="topic-row__index">
                {String(i + 1).padStart(2, '0')}
              </div>
              <div class="topic-row__body">
                <h2>
                  <a href={`/topics/${topic.slug}/`}>{topic.title}</a>
                </h2>
                <p class="topic-row__summary">{topic.summary}</p>
                <ul class="topic-row__tags">
                  {topic.whatMatters.slice(0, 4).map((d) => (
                    <li>{d.split('—')[0].trim()}</li>
                  ))}
                </ul>
                <a class="card__link" href={`/topics/${topic.slug}/`}>
                  Notes on {topic.title} &rarr;
                </a>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <CTA />
</BaseLayout>

<style>
  .topic-list {
    display: grid;
    gap: 0;
    border-top: 1px solid var(--border);
  }

  .topic-row {
    display: grid;
    grid-template-columns: 4rem 1fr;
    gap: clamp(1rem, 3vw, 2.5rem);
    padding-block: clamp(2rem, 4vw, 2.75rem);
    border-bottom: 1px solid var(--border);
  }

  @media (max-width: 620px) {
    .topic-row {
      grid-template-columns: 1fr;
      gap: 0.75rem;
    }
  }

  .topic-row__index {
    font-size: 0.85rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: var(--accent);
    padding-top: 0.45rem;
  }

  .topic-row h2 {
    font-size: var(--step-2);
    margin-bottom: 0.75rem;
  }

  .topic-row h2 a {
    text-decoration: none;
  }

  .topic-row h2 a:hover {
    color: var(--accent);
  }

  .topic-row__summary {
    color: var(--text-muted);
    font-size: var(--step-0);
    max-width: 62ch;
  }

  .topic-row__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    list-style: none;
    padding: 0;
    margin-top: 1.15rem;
  }

  .topic-row__tags li {
    font-size: 0.82rem;
    padding: 0.35rem 0.85rem;
    border-radius: 999px;
    background: var(--bg-alt);
    border: 1px solid var(--border);
    color: var(--text-muted);
  }
</style>
